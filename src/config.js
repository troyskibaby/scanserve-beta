const config = {
    apiUrl: process.env.REACT_APP_API_URL, // e.g., "/api" for local, full URL for production
    origin: process.env.REACT_APP_ORIGIN,    // e.g., "http://localhost:3000"
    key: process.env.REACT_APP_API_KEY
    // You can add additional settings here as needed.
  };
  
  export default config;
  